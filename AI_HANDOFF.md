# AI間連携ハンドオフファイル (Phase 2)
> **作成者**: GitHub Copilot (リーダー担当)
> **対象**: もう一方のAI
> **最終更新**: 2026-03-07

---

## あなたへの指示

> **進捗更新 (Leader)**: Phase 2 の3タスクは実装完了済みです。以降はレビューとテスト中心で対応してください。

このファイルのタスクは必要時のみ再実装し、通常はレビュー結果だけを `AI_STATUS.md` に追記してください。

---

## 今回の目的

初期実装は完了済みです。今回は運用事故を防ぐための「通知信頼性」と「問い合わせ体験」の強化を行います。

---

## タスク 1 (High): LINEフォールバック失敗の再試行キュー実装

**対象**: `c:\Users\motoc\new-blog-system\app.py`（必要なら `src/models.py` も更新）

**要件**:
- メール送信失敗時のLINEフォールバックが失敗した場合、失敗内容を保存する仕組みを追加
- 最小要件: DBテーブルまたはJSON永続化のどちらかで記録
- 再試行は最大3回（指数バックオフ推奨）
- ログに `fallback_retry` の識別子を残す

**完了条件**:
- 失敗通知が保存される
- 再試行処理を手動で実行できる関数/エンドポイントがある
- 3回失敗時に「要手動対応」と判別可能

---

## タスク 2 (High): pTIMER投稿時の git push リトライ

> **進捗更新 (Leader)**: このタスクは 2026-03-07 に Leader が先行実装済みです。再実装は不要。必要ならレビューのみ実施してください。

**対象**: `c:\Users\motoc\ptimer\ptimer_app\api\app.py`

**要件**:
- `post-dev-diary` の git push 部分にリトライ（最大3回）を追加
- push失敗時に `git_pushed=false` だけで終了せず、最終失敗理由をレスポンスへ明示
- 可能なら記事URLの存在確認（HTTPステータス確認）を追加

**完了条件**:
- 一時的なpush失敗で自動再試行される
- 全失敗時にレスポンスから原因判別できる

---

## タスク 3 (High): 問い合わせ送信者への自動返信メール

**対象**: `c:\Users\motoc\new-blog-system\app.py`

**要件**:
- `/api/contact` 成功時に、問い合わせ送信者 (`email`) へ受領メールを自動送信
- 件名例: `お問い合わせありがとうございます - Humanoid Media Factory`
- オーナー通知メールとは別処理にする
- SMTP失敗時は既存方針と同様にLINEフォールバックを活用（可能な範囲で）

**完了条件**:
- API成功時に送信者向け受領メールが送られる
- 失敗時のログで owner向け通知失敗と送信者向け失敗を区別できる

---

## 変更ルール

- 既存機能を壊さない（特に `/api/newsletter/send` と `/api/contact`）
- 環境変数名は既存互換を優先（`SMTP_PASS` / `SMTP_PASSWORD`）
- 変更後、最低限の実行確認を行う（構文エラーなし）

---

## 完了後に必ず更新する項目

`c:\Users\motoc\chnmotoTmz.github.io\AI_STATUS.md` の **Other AI (Phase 2)** を更新してください。

```markdown
## Other AI (Phase 2)
- LINE再試行キュー: ✅ / 🔄 / ❌
- pTIMER git push リトライ: ✅ / 🔄 / ❌
- contact 自動返信メール: ✅ / 🔄 / ❌
```

---

*このファイルはGitHub Copilot (Leader)が更新しました。*
